<h6>Dashboard</h6>
<h3>Welcome {{name}}</h3>
<p>Here are your stories!</p>

{{#if stories}}
    <table class="striped">
        <thead>
            <tr>
                <th>Title</th>
                <th>Date</th>
                <th>Status</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            {{#each stories}}
                <tr>
                    <td><a href="/stories/{{_id}}">{{title}}</a></td>
                    <! –– using our helper js file, hbs.js we can manipulate createAt this way ––>
                    <td>{{formatDate createdAt 'MMMM Do YYYY, h:mm:ss a'}}</td>
                    <td><span class="dash-status">{{status}}</span></td>
                    <td>
                        <a href="/stories/edit/{{_id}}" class="btn btn-float">
                        <i class="fas fa-edit"></i>
                        </a>

                        <! –– not really gonna be a POST, will use the override method to DELETE ––>
                        <form action="/stories/{{_id}}" method="POST" id="delete-form">
                        <! –– same as before, purpose is to delete ––>
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn red">
                            <!–– turns out i stands for icon ––>
                            <i class="fas fa-trash"></i>
                        </button>
                        </form>
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>
{{else}}
    <p>You have not created any stories</p>
{{/if}}